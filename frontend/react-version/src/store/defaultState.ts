import { UserState } from '../types/UserState';

export const defaultUserState: UserState = {
  user: {},
};

export const defaultUtilitiesState: any = {
  modal: {
    title: 'asfafaafasf',
    component: 'UserInformation',
    visible: true,
  },
};
