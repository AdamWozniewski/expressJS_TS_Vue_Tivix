{
  "name": "tivix",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend/vue_version && npm run serve",
    "install:backend": "npm --prefix ./backend install",
    "install:frontend": "npm --prefix ./frontend/vue_version install",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "AdamW",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "eslint": "^7.25.0"
  }
}
