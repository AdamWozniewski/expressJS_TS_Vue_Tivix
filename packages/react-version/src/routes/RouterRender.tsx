import React from 'react';
// import { Route } from 'react-router-dom';

export const RouterRender = ({ routes }: { routes: any }) => {
  // return (
  //   <Route path={`${routes.path}`} render={({ match: { url } }) => (
  //     <>
  //       {routes.children.map(({ path, component, exact }: any, index: number) =>
  //         <Route
  //           key={index}
  //           exact={exact || false}
  //           path={`${routes.path}${path}`}
  //           component={ component }
  //         />
  //       )}
  //     </>
  //   )} />
  // );
  return <div>abc</div>;
};
